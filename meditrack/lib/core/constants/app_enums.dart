enum ExerciseField {
  reps,
  weight,
  assistedWeight,
  extraWeight,
  bodyWeight,
  duration,
  distance,
  calorieBurned,
}

enum WorkoutType { admin, custom, shared }

enum InfoWidgetType { tag, muscleGroup }

enum ExerciseDetailLayout { simple, tabbed }
